# 地域課題マップ・市町村カルテシステム

大阪府 市町村局振興課 内部職員向けの、地域課題マップ・住民目線マップ・市町村カルテ・分析レポートを提供するツールです。
`app.html` を直接ブラウザで開くだけで、そのまま動作します（サーバー・ログイン不要）。

**スコープ**：地域課題マップ（行政の計画視点、7指標）／住民目線マップ（住民の生活視点、4指標）／防災マップ（災害リスク、4指標）／一覧表（並べ替え・CSV保存）／市町村カルテ／分析レポートの6機能。市町村カルテ・分析レポート・一覧表は印刷用レイアウトに対応。
相談・訪問履歴管理、ダッシュボード、アンケート結果分析、認証・バックエンド接続は対象外です。

---

## 1. システム構成

```
┌──────────────────────────────────────────────────────────┐
│                     ブラウザ（PC・タブレット）                │
│  app.html（単体ファイル・サーバー不要）                       │
│   ├─ 市町村統計データ：e-Stat・総務省・大阪府の実データを埋め込み │
│   ├─ 市町村境界GeoJSON：国土数値情報(N03)由来のデータを埋め込み  │
│   ├─ 課題スコア：統計データから決定的に算出（JS内ロジック）      │
│   └─ 分析レポート：ルールベースのローカル生成（外部API呼び出し無し） │
└───────────────────────────────┬────────────────────────────┘
                                 │ HTTPS（タイル画像の取得のみ）
┌───────────────────────────────▼────────────────────────────┐
│  外部サービス：国土地理院 地理院タイル（地図背景画像の配信のみ）  │
└──────────────────────────────────────────────────────────┘
```

**ポイント**
- サーバー・データベース・ログインは無し。認証やアクセス制御が必要になった時点で、ホスティング先
  （社内ファイルサーバー、簡易な社内Webサーバー等）側のアクセス制限で対応する想定。
- 地図の背景は**国土地理院の地理院タイル「淡色地図」**（`https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png`）をLeafletで表示する。標準地図（等高線・道路網が密）は着色データと競合して見づらいため、データ可視化用に作られた淡色地図を採用している。利用は無償・APIキー不要だが、地理院タイルの利用規約に従い「地図：国土地理院」の出典表示を行う。
- 市町村は**コロプレス（境界ポリゴンの塗り分け）**で表示する。境界データは国土数値情報「行政区域データ（N03）」を基にしたGeoJSON（大阪市・堺市は区データを結合）を使用し、選択した指標のスコアで塗り分ける。円マーカーではなく実際の行政区域ポリゴンなので、都市部で市町村が重なって見えなくなる問題が発生しない。
- 地図の表示・パン可能範囲は、このポリゴンの外形（`getBounds()`）にフィットさせて固定する。これにより初期表示・パン・ズームのいずれでも近隣府県が大きく映り込まず、大阪府内に視点が留まる。
- 市町村統計は複数の公的統計の実データ（人口・高齢化率・世帯数・財政力指数等はe-Stat「社会・人口統計体系」、空き家率は総務省統計局「住宅・土地統計調査」、土砂災害警戒区域数は大阪府、浸水想定区域は国土数値情報、生産年齢人口・事業所数はe-Stat「社会・人口統計体系」。公共交通は2020年国勢調査「利用交通手段」の自家用車利用率、DXは総務省のAI・RPA・生成AI導入状況調査（令和7年度）と「自治体DX・情報化推進概要」の手続オンライン化状況（令和7年4月1日現在））。課題スコア（7指標）と災害リスク（4指標）はそこから決定的に算出する（`computeScores`関数）。推定値による代用はどの指標でも行っていない。詳細はアプリ内「指標の計算方法」画面を参照。
- 住民目線マップの4指標はすべて実データ。「買い物の不便さ」は農林水産省「食料品アクセスマップ」（2020年）、「医療への行きにくさ」「学校への通いにくさ」は可住地面積当たりの施設密度としてe-Stat「社会・人口統計体系」から算出。残り1指標「地元で買えない度」（支出流出入率）と、市町村カルテの「将来人口（2050年推計）」「業種別事業所数」は、RESAS（地域経済分析システム）のAPIが2025年3月に提供終了しているため、RESASの新サイト（resas.go.jp）の「地域ビジネス環境分析」を市区町村ごとに開いて取得した実データ。
- 分析レポートは外部APIを呼ばず、地域課題マップ7指標・住民目線マップ4指標・防災マップ4指標のスコアから共通課題・特徴的な課題・住民目線で見た課題・災害リスク・支援方針・広域連携の共同検討の候補をルールベースで生成する。比較には1〜5に丸める前の連続値（`m.rawScores` / `m.residentRawScores` / `m.hazardRawScores`）を使い、2つの課題が同時に高い組み合わせ（`COMBO_RULES`）と、同規模グループの中で悪化が目立つ変化（`TRENDS`）も判定する。「特徴的な課題」は人口規模が近い団体（`m.peerGroup`：政令市・中核市／人口10万人以上の一般市／10万人未満の一般市／町村）の平均と比べ、「広域連携」は境界を接する市町村（`ADJACENT`、国土数値情報N03の境界から算出）で共通して高い課題を名指しで挙げる。ブラウザへの保存（localStorage等）は一切行わない、完全な表示専用ツール。

---

## 2. 画面一覧

| # | 画面名 | 概要 |
|---|---|---|
| 1 | 地域課題マップ | 国土地理院地図（大阪府内に表示範囲を固定）上に市町村境界を7指標（行政の計画視点）でスコア化・色分け表示。ポップアップに指標名・整数スコア・丸め前スコア・元の統計値と、折りたたみの「判定根拠を表示」（年度・基準値・出典・区分＝実測値／推計値／加工値／代理指標、`INDICATOR_META`）を表示 |
| 2 | 住民目線マップ | 同じ地図を、買い物の不便さ・医療への行きにくさ・学校への通いにくさ・地元で買えない度の4指標（住民の生活視点）でスコア化・色分け表示 |
| 3 | 防災マップ | 同じ地図を、土砂災害・水害・地震の揺れ・液状化の4指標（災害リスク）で色分け表示。総合は4つのうち最も高いもの |
| 4 | 一覧表 | 43市町村×15指標（＋総合）を1つの表に並べ、列で並べ替え。スコアと元の統計値を切り替え、CSV（スコア＋元の値、BOM付きUTF-8）で保存 |
| 5 | 市町村カルテ | 市町村ごとの基礎情報（RESAS将来人口・業種別事業所数を参考表示）・課題スコア（地域課題マップ7指標＋住民目線マップ4指標＋防災マップ4指標）。上部のページ内ナビで概要・地域課題・住民目線・防災・産業へ移動。市町村の選択は地域ブロック順・自治体コード順・五十音順を切り替え（分析レポートの対象選択も同じ） |
| 6 | 分析レポート | 範囲（全域／地域ブロック／市町村）を選んで分析を生成 |
| 7 | 指標の計算方法 | データソースと15指標の算出方法を説明する静的な参照画面（サイドバーから常時アクセス可） |

---

## 3. 画面遷移図

```
[地域課題マップ] ─┐
[住民目線マップ] ─┴─市町村をクリック（ポップアップ）──▶ [市町村カルテ] ──戻る──▶ (元のマップ)
                                                             │
                                                             └─▶ [分析レポート（市町村指定）]

[分析レポート：範囲選択] ──生成──▶ [分析レポート：結果表示]
```

---

## 4. データ構造

サーバー・DBを持たないため、`app.html`内のJavaScriptオブジェクトがそのままデータモデルです。

### 4.1 市町村マスタ（`RAW` → `MUNIS`）
| 項目 | 説明 |
|---|---|
| id | 短縮ID（例: `osaka`） |
| name | 市町村名 |
| region | 地域ブロック（大阪府の地域区分：大阪市域／豊能／三島／北河内／中河内／南河内／泉北／泉南） |
| lat, lng | 代表座標（ポップアップ位置の目安。地図の塗り分け自体はGeoJSON境界を使用） |
| population | 人口（2020年国勢調査） |
| popChangeRate | 人口増減率%（2015→2020年の年率換算） |
| agingRate | 高齢化率%（65歳以上人口／総人口） |
| areaKm2 | 面積km2 |
| households | 世帯数 |
| fiscalIndex | 財政力指数（総務省、直近公表年度） |
| vacantRate | 空き家率%（住宅・土地統計調査。小規模5町村は`null`） |
| aloneElderlyRate | 65歳以上単独世帯率%（国勢調査） |
| workAgeChange | 生産年齢人口（15〜64歳）の増減率%、2015→2020（`WORK_AGE_CHANGE`）。担い手不足の入力 |
| est2016 / est2021 / estChange | 民営事業所数と増減率%、2016→2021（`ESTABLISHMENTS`）。カルテ・分析レポートの「変化」のみ |
| agingRise | 高齢化率の上昇幅、2015→2020（`AGING_RISE`）。分析レポートの「変化」のみ |
| floodExposure / floodRiver / floodSurge / floodTsunami | 床上浸水以上の浸水想定区域に住む人の割合%（`FLOOD_EXPOSURE`。国土数値情報A31a・A49・A40＋国勢調査250mメッシュ人口を50m四方に等分）。防災マップの入力 |
| quakeProb / quakeProb6s | 30年以内に震度6弱以上／6強以上の揺れに見舞われる確率%の人口加重平均（`QUAKE_HAZARD`。J-SHIS確率論的地震動予測地図2024年版・250mメッシュ）。6強以上が防災マップ「地震の揺れ」の入力 |
| liquefyRate | 液状化しやすい地形（国土交通省の液状化発生傾向「強い」「やや強い」）に住む人の割合%（`QUAKE_HAZARD`。J-SHIS 250mメッシュ微地形区分）。防災マップの入力 |
| cardRate | マイナンバーカード保有率%（総務省、令和8年8月末）。カルテの参考表示のみ（スコア化なし） |
| dxAi / dxRpa / dxGenai | AI・RPA・生成AIの導入状況（`DX_ADMIN`。2=導入済・1=実証中・0=未導入。総務省、令和7年度） |
| onlineDone / onlineApplicable / onlineDoneRate | 重点計画対象手続のうちオンライン化済の数・対象数・割合%（`DX_ADMIN`。総務省「自治体DX・情報化推進概要」、令和7年4月1日現在） |
| hazardCount | 土砂災害警戒区域数（大阪府指定） |
| carCommuteRate | 通勤・通学の自家用車利用率%（`CAR_COMMUTE_RATE`。2020年国勢調査「利用交通手段」、e-Stat統計表0003454514）。公共交通指標の入力。 |
| railCover1km | 鉄道駅1km圏に住む人の割合%（`RAIL_COVER_1KM`。250mメッシュ人口から算出。カルテの参考表示のみ、スコア化なし） |
| elderlyAloneShare / elderlyOnlyHH | 65歳以上のうち一人暮らしの割合%・高齢者のみの世帯（単身＋夫婦のみ）の割合%（`ELDERLY_HOUSEHOLDS`、2020年国勢調査）。カルテの参考表示のみ（スコア化なし） |

### 欠損値の扱い
非公表値はデータなしとして扱い、推定値や中間値による補完は行わない。総合スコアは利用可能な指標だけで算出する。
- 対象：空き家率（未公表の5町村）、支出流出入率（`RESAS_LEAKAGE`が`null`の2市＝河内長野市・東大阪市）。スコア（整数・丸め前とも）も`null`。
- 表示：地図はグレー、ポップアップ・カルテ・一覧表は共通の「データなし」表示（`naHtml()`）。レーダーチャートは点を打たず（0として描かない）、軸名に「（データなし）」を付ける。CSVでは空欄。
- 集計：総合スコアはデータのある指標だけの平均。分析レポートの共通課題・同規模比較・課題の重なり・広域連携の判定では`null`を除外する。

### 4.2 課題スコア（`computeScores(m)`が返す7指標、1〜5）
`population / aging / vacant / transit / community / successor / dx` の各キーで、
上記の実データから決定的に算出する（計算式はアプリ内「指標の計算方法」画面参照）。7指標すべて公的統計の
実データで、推定値による代用はしていない。`vacant`は`vacantRate`が`null`の
5町村（能勢町・田尻町・岬町・太子町・千早赤阪村）では`null`となり、地図はグレー表示、`m.composite`
（総合スコア）の算出からも除外される（推定値による代用はしていない）。

災害リスクは`computeHazardRawScores(m)`が`landslide / flood / quake / liquefy`の4キーで返し（`m.hazardRawScores`、
整数は`m.hazardScores`）、4つのうち最も高いものを`m.hazardTop`（種類）・`m.hazardMax`（点数）に持つ。自然条件としての
危険度で社会の変化から生じる課題とは性質が違うため、`m.composite`には含めない。

### 4.3 住民目線データ（`RESIDENT_RAW` → `MUNIS`の各プロパティ）
「人口当たりの施設数」で測ると、人口が減った町村ほど見かけ上「1人当たりの施設が多い＝恵まれている」と
出てしまい住民の実感と逆になる（例：千早赤阪村は人口4,909人・診療所5軒で「人口10万人当たり102施設」と
府内最上位になるが、実際には村内の移動距離が長い）。そのため住民目線マップの指標は「供給量」ではなく
**行きにくさ（距離・密度）**で測る形に組み直している。人口を母数から外し、可住地面積当たりの施設密度と、
国が実測した買い物困難人口の割合を使う。

| 項目 | 説明 |
|---|---|
| foodAccessRate | 食料品アクセス困難人口割合%（65歳以上、農林水産省「食料品アクセスマップ」2020年）。店舗まで直線500m以上かつ自動車利用が困難な高齢者の割合。 |
| clinicDensity | 一般診療所数（可住地面積100km²当たり、2023年度） |
| schoolDensity | 小学校＋義務教育学校数（可住地面積100km²当たり、2024年度、学校基本調査） |
| habitableKm2 | 可住地面積km²（総面積から林野・湖沼を除いた面積） |
| leakageRate | 支出流出入率%（RESAS「地域ビジネス環境分析」、2022年。非公開の2市は`null`で、`residentScores.leakage`も`null`＝データなし。住民目線の総合は残り3指標の平均） |

`residentScores`（`food / clinic / school / leakage`、1〜5）と`residentComposite`（単純平均）も
`MUNIS`の各要素に持たせている。`food`のみ値が大きいほど課題が大きい（`residentScaleScoreUp`）、
残り3指標は値が悪い（施設密度が低い・支出が域外へ流出）ほどスコアが高くなるよう反転している
（`residentScaleScore`）。

施設密度の2指標（`clinic / school`）は、市街地と山間部で100倍以上開くため**常用対数（log10）**に変換して
から点数化する。また可住地面積が小さい市町村では施設1件の増減で密度が大きく振れるため、可住地面積を
母数とした**小規模自治体補正**（`shrinkDensity`関数、信頼度加重平均＝縮小推定）をスコア化の前にかけ、
`m.clinicDensityAdj / m.schoolDensityAdj`として保持する。経済指標である`leakageRate`はこの補正の対象外
（計算式は「指標の計算方法」画面参照）。

`NURSING_PER1K` → `m.nursingPer1k`（介護施設定員／65歳以上人口千人）は、広域から利用者を受け入れる大規模
施設が小規模町村に1つあるだけで数値が跳ね上がり「その町の住民が受けやすい」ことを意味しないため、
スコア化せず市町村カルテの参考値としてのみ表示する。図書館数も大阪市・堺市以外ほぼ全市町村で0〜3館しか
なく市町村間の比較に耐えないため、指標から外している。

### 4.4 RESASデータ（`RESAS_POP2050` / `RESAS_LEAKAGE` / `RESAS_INDUSTRY`）
RESASのAPIは2025年3月に提供終了しているため、RESASの新サイト（resas.go.jp）の「地域ビジネス環境分析」を
市区町村ごとに開いて取得した実データ。`RESAS_LEAKAGE`は上記`leakageRate`の元データ。

| 項目 | 説明 |
|---|---|
| `RESAS_POP2050` → `m.pop2050` / `m.popRate2050` | 2050年推計人口（人）、2020→2050年増減率%。市町村カルテの基本指標に参考表示（スコア化なし）。 |
| `RESAS_INDUSTRY` → `m.industry` | 生活関連12業種の事業所数`[2016年,2021年]`の配列（`INDUSTRY_LABELS`と対応）。事業所数1〜2件の業種はRESAS側の秘匿処理で`null`。全業種が非公開の市町村（交野市）は配列全体が`null`。市町村カルテに参考表示（スコア化なし）。 |

### 4.5 市町村境界（`OSAKA_GEOJSON`）
GeoJSON `FeatureCollection`。各Featureの`properties.id`が4.1の`id`と対応する。大阪府43市町村分。地域課題マップ・
住民目線マップの両方で共有する。

---

## 5. ファイル構成

```
docs/chiiki-karte-system/
├─ README.md   本書
├─ app.html    実装一式（HTML/CSS/JSを1ファイルに集約。指標の計算方法の説明画面も同ファイル内）
└─ tools/      埋め込みデータを作り直すスクリプト（データ更新手順は tools/README.md）
   ├─ download.sh   地理データ・公開ファイルの取得
   ├─ estat.py      e-Stat API（自家用車利用率・生産年齢人口・高齢化率の上昇幅・事業所数）
   ├─ geo.py        浸水・地震・液状化・鉄道駅1km圏の人口割合、隣接市町村
   ├─ dx.py         AI・RPA・生成AI導入状況、手続のオンライン化状況
   └─ common.py     共通処理（市町村一覧・メッシュ計算・境界の読み出し）
```

データは年度ごとに更新が必要です。手順と、スクリプト化していない手作業のデータの出典は
[`tools/README.md`](tools/README.md) にまとめています。

`app.html`内部の主なセクション（`<script>`内のコメント区切りに対応）:

```
市町村データ（RAW配列、e-Stat・総務省・大阪府の実データ）
公共交通データ（CAR_COMMUTE_RATE / RAIL_COVER_1KM、国勢調査・国土数値情報）
行政DXデータ（DX_ADMIN、総務省）
担い手・変化・防災データ（WORK_AGE_CHANGE / ESTABLISHMENTS / AGING_RISE / FLOOD_EXPOSURE / QUAKE_HAZARD）
市町村境界GeoJSON（OSAKA_GEOJSON）
住民目線データ（RESIDENT_RAW / NURSING_PER1K、農水省・e-Stat実データ）
RESASデータ（RESAS_POP2050 / RESAS_LEAKAGE / RESAS_INDUSTRY、RESASサイトから取得した実データ）
ルーティング／状態（画面切り替え）
地図（国土地理院タイル + Leaflet コロプレス。3つのマップを createChoropleth で生成）
市町村カルテ
分析レポート（ルールベース生成）
初期化
```

---

## 6. 今後の拡張余地

複数職員での共有・編集や、認証が必要になった場合は、以下のような追加が考えられます（現状は未実装）。

- **担当者メモ・引継ぎ記録**：市町村ごとのメモを職員間で共有したい場合は、簡易なバックエンド（Supabase等）を保存先として追加する。ブラウザの`localStorage`は端末をまたいで共有されないため、単体ファイルのまま実装する選択肢は取らない。
- **アクセス制御**：社内ネットワーク限定公開、または簡易なBasic認証をホスティング側で設定する。
- **分析レポートの高度化**：`computeScores`と同じ入力データを使い、OpenAI等のLLM APIを呼ぶサーバー側の中継エンドポイントを追加する（APIキーをブラウザに置かないため）。
- **相談・訪問履歴管理・アンケート分析・ダッシュボード**：今回のスコープ外だが、同じ市町村マスタ・スコアを土台に追加可能。

---

## 7. 実装コード一式

`app.html`をブラウザで直接開くと、地域課題マップ／住民目線マップ（どちらも国土地理院地図＋Leafletのコロプレス、大阪府内に表示範囲固定）／市町村カルテ／分析レポート（ルールベース生成）が一通り動作します。

- 地図は実際に国土地理院の地理院タイル（淡色地図）を読み込みます。市町村は実際の行政区域境界ポリゴン（国土数値情報N03由来）で塗り分けます。表示・パン範囲はそのポリゴン外形にフィットさせて大阪府内に固定しているため、近隣府県が大きく画面を占めません。市町村をクリックするとポップアップから市町村カルテに遷移します。
- 統計データは複数の公的統計の実データ（大阪府43市町村の人口・高齢化率・世帯数・面積・財政力指数・空き家率・生産年齢人口の増減・民営事業所数・浸水想定区域の人口割合・震度6強以上の確率・液状化しやすい地形の人口割合・マイナンバーカード保有率・AI／RPA／生成AI導入状況・手続のオンライン化状況・土砂災害警戒区域数・通勤の自家用車利用率・食料品アクセス困難人口割合・可住地面積当たり診療所数／学校数・介護施設定員・支出流出入率）。指標ごとの算出方法はアプリ内「指標の計算方法」画面にまとめている。市町村カルテの「将来人口（2050年推計）」「業種別事業所数」はRESASサイトから取得した参考データ（スコア化なし）。

**注記**：Claude Artifactのプレビュー（claude.ai上のホスト環境）では、セキュリティ上の制約により地理院タイルサーバーへの画像リクエストがブロックされ、地図の背景タイルが表示されない場合があります。`app.html`をこのリポジトリからダウンロードして通常のブラウザで直接開けば、この制約は適用されず正しく表示されます。
